---
title: 快速排序
tags:
  - 算法
abbrlink: ff8068c0
date: 2017-09-19 01:21:32
---
排序算法有很多，桶排序、冒泡排序、快速排序、计数排序、基数排序、插入排序、归并排序和堆排序等等。
详细理解先空着，先给出例子。等理解深了再来补充。
<!-- more -->
```
// 快速排序
// 原理: 每次排序的时候设置一个基准数，将小于等于基准点的数全部放到基准点的左边，将大于等于基准点的数全部放到基准点的右边。
// 其实快速排序是基于一种叫做“二分”的思想。
function quickSort($arr)
{
    // 如果非数组  返回false
    if (!is_array($arr)) {
        return false;
    }
    $len = count($arr);
    // 如果数组只有一位数  则返回当前数组
    if ($len <= 1) {
        return $arr;
    }
    // 基准数
    $baseNum = $arr[0];
    // 创建两个空数组  分别装左右两边的数值
    $leftArr = $rightArr = [];
    for ($i=1; $i < $len; $i++) {
    	// 如果基准数大于当前值 则放在左边数组  反之右边
        if ($baseNum > $arr[$i]) {
            $leftArr[] = $arr[$i];
        } else {
            $rightArr[] = $arr[$i];
        }
    }
    // 递归
    $leftArr = quickSort($leftArr);
    $rightArr =quickSort($rightArr);
    // 合并数组并且返回
    return array_merge($leftArr, array($baseNum), $rightArr);
}

$arr = [123,12312,4,312,2352,3532,123,1231];
var_dump(quickSort($arr));
```